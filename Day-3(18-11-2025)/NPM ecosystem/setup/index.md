1. Install Node.js
# node -v
# npm -v

2. Create package.json
mkdir my-npm-project
cd my-npm-project

npm init -y (creates-package.json)
npm install express **it add automaticaly express dependency**


node index.js
# or via npm script (add to package.json scripts: "start": "node index.js")
npm start
